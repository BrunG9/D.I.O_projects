let compair = (num1, num2) => {
    if (num1 === num2 && (num1+num2 > 10 || num1+num2 < 20))
    {
        return `Os numeros ${num1} e ${num2} não sao iguais. 
        Sua soma é ${num1+num2}, que é menor que 10 e menor que 20`
    }
}